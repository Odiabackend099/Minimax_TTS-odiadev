# Production Requirements for OdeaDev-AI-TTS
# Use this for production deployments with all security and performance enhancements

# ==================== Core Framework ====================
fastapi==0.109.0
uvicorn[standard]==0.27.0

# ==================== Database ====================
sqlalchemy==2.0.25
alembic==1.13.1  # Database migrations
psycopg2-binary==2.9.9  # PostgreSQL driver (comment out if using SQLite only)

# ==================== Data Validation ====================
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# ==================== HTTP Client (Async) ====================
httpx==0.26.0  # Async HTTP client (replaces requests)
h2==4.1.0  # HTTP/2 support

# ==================== Security ====================
bcrypt==4.1.2
argon2-cffi==23.1.0  # Stronger password hashing (optional upgrade)
python-jose[cryptography]==3.3.0  # JWT handling
cryptography==42.0.0

# ==================== Configuration ====================
python-dotenv==1.0.0

# ==================== Rate Limiting ====================
slowapi==0.1.9  # Rate limiting for FastAPI
redis==5.0.1  # Redis client for distributed rate limiting
hiredis==2.3.2  # Fast Redis parser

# ==================== Caching ====================
cachetools==5.3.2  # In-memory caching
aiocache==0.12.2  # Async cache

# ==================== Retry & Circuit Breaker ====================
tenacity==8.2.3  # Retry logic with exponential backoff

# ==================== Monitoring & Logging ====================
structlog==24.1.0  # Structured logging
python-json-logger==2.0.7  # JSON log formatting
prometheus-client==0.19.0  # Metrics
sentry-sdk[fastapi]==1.39.2  # Error tracking (optional)

# ==================== Testing ====================
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx-mock==0.16.0  # Mock httpx requests
faker==22.0.0  # Generate test data

# ==================== Utilities ====================
python-multipart==0.0.6  # Form data support
bleach==6.1.0  # HTML sanitization
validators==0.22.0  # Input validation

# ==================== Development Tools (optional) ====================
black==24.1.0  # Code formatting
ruff==0.1.14  # Fast linter
mypy==1.8.0  # Type checking
pre-commit==3.6.0  # Git hooks

# ==================== Production Server (alternative to uvicorn) ====================
# gunicorn==21.2.0  # Production WSGI server
# uvloop==0.19.0  # Faster event loop
# httptools==0.6.1  # Faster HTTP parsing

# ==================== Optional: Enhanced Features ====================
# celery==5.3.4  # Background tasks
# flower==2.0.1  # Celery monitoring
# APScheduler==3.10.4  # Task scheduling
